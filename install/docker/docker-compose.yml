services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    volumes:
      - redis_data:/data

  yttrans:
    build:
      context: ../..
      dockerfile: install/docker/Dockerfile
    restart: unless-stopped
    depends_on:
      - redis
    ports:
      - "9095:9095"
    env_file:
      - ../../.env
    environment:
      YTTRANS_QUEUE_REDIS_URL: redis://127.0.0.1:6379/0
    volumes:
      - ../..:/opt/yttrans
      - ../../.env:/opt/yttrans/.env:ro
      - hf_cache:/opt/yttrans/.cache/huggingface

volumes:
  redis_data:
  hf_cache:
